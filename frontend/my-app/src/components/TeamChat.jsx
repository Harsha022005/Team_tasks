import React from "react";
import TeamChat from "../pages/Teams_Chat_app";

const TeamChatWrapper = (props) => {
  // Pass all props to TeamChat
  return <TeamChat {...props} />;
};

export default TeamChatWrapper;
